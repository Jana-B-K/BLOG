<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Blog</title>
    <link rel="stylesheet" href="styles/main.css">
</head>
<body>
    <div class="container">
        <h1><%= heading %></h1>

        <% if(locals.post){%>
            <form id="editForm" action="/api/posts/<%= post.id %>" method="post">
                <input type="text" name="title" value="<%= post.title %>" id="">
                <textarea name="content" id="" cols="30" rows="10">
                    <%= post.content %>
                </textarea>
                <input type="submit" name="author" value="<%= post.author %>">
                <button class="full-width" type="submit">
                    <%= submit %>
                </button>
            </form>
        <%} else{%>
            <form id="newPOstForm" action="/api/posts" method="post"> 
                <input type="text" name="title" placeholder="Title" required>
                <textarea name="content" id="" cols="30" rows="10" placeholder="Content" required></textarea>
                <input type="text" name="author" placeholder="Author" id="">
                <button class="full-width" type="submit">
                    <%= submit %>
                </button>
            </form>
        <%}%>
    </div>
</body>
</html>